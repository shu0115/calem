%h1
  Welcome to #{Settings.app_name}.

%br

- if current_user.present?
  %p= link_to "Logout", logout_path, class: 'btn btn-primary'
- else
  %p= link_to "Login", "/auth/#{Settings.provider}", class: 'btn btn-primary'
